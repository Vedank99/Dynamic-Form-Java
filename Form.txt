import formElements.*
import createFormUsingSwing

class Form{
	final title; //Title cannot be changed
	final arraylist<formElements> children; //Form elements
	
	Form(string title){
		this.title = title;
		children = new Arraylist[];
	}		

	main(){
		string title;
		cout>>Enter form title;
		cin>>title;
		Form myForm = new Form(title);
		
		bool formEnd = false;
		while(formEnd == false){
			
			int elementNumber;
			
			cout<<Choose form element;
			cout<<1) TextView
			cout<<2) Radio Button
			cout<<3) CheckBox
				so on
			cout<<n) End Form
			
			cout<<Enter the number for element
			cin>>elementNumber
			
			switch(elementNumber){
				
				Case 1:
					formElements.TextView textview = new formElements.TextView();
					myForm.children.add(textview);
					break;
				Case 2:
					formElements.RadioButton radioButton = new formElements.RadioButton();
					myForm.children.add(radioButton);
					break;
				etc
				
				Case n:
					formEnd = true;
				
			}

						
		}
		
		createFormUsingSwing.createForm(myForm);		
	}
	
}