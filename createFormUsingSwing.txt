//Here we write the code for creating form using Swing and storing its data

import Form
import Swing
import JsonParser
import formElements

class createFormUsingSwing{
	
	Form myForm; 
	
	createFormUsingSwing(Form form){
		
		myForm = form;	
	}
	
	createForm(){
		//Creates form
	
		swing.setTitle(myForm.title);
			
		for(int i = 0;i<form.size();i++){
			switch(form.children[i]){
				
				case instanceOf TextView :
				//Swing code to create textview
				
				etc.
			}
		} 
	}	 
	
	handleInput(input){
		//handles input
		JsonParser.parse(input);
		JsonParser.save();
	}
		
	main(){
		saveForm(myForm); //Saves form as a FormTitle.java file. Uses fwrite method of Java.
		
	}
}